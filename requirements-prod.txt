# Production dependencies only
# This file contains only runtime dependencies (no testing/linting tools)

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.22  # For file upload support

# Data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1

# Image processing
pillow==10.2.0
numpy>=1.26,<3    # Vectorised LAB colour matching

# PDF generation
reportlab==4.0.9

# Logging
structlog==24.1.0

# HTTP client (for health checks)
httpx==0.26.0
